name: Run bash script
run-name: run bash script
on: [push]
jobs:
  run_bash_script:
    runs-on: ubuntu-latest
    steps:
      - name: run setup.sh
        run: |
          echo "test1"
          ls -a
          sh setup.sh